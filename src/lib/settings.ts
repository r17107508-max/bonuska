import { getDb } from "@/lib/db";

const defaultOfferText = `Договор-оферта SaaS-сервиса программы лояльности

Это предварительный шаблон, который должен проверить юрист.

Исполнитель: самозанятый Алиуллов Раиль Фаридович, ИНН 732711876598, телефон 89278370717, email rf173@bk.ru.

Сервис предоставляется как есть, без обещания бесперебойной работы 24/7. Исполнитель не отвечает за внутренние акции компаний, законность рекламы, фактическую выдачу подарков клиентам и сделки между компанией и клиентом. Компания самостоятельно отвечает перед своими клиентами.

Trial составляет 14 дней. Платный период составляет 30 дней. Стоимость базового тарифа - 499 рублей в месяц. Оплата за уже оплаченный период не возвращается, кроме случаев, прямо предусмотренных законом. При неоплате, злоупотреблениях или нарушении условий доступ может быть ограничен. Применяется законодательство РФ.`;

const defaultPrivacyText = `Политика обработки персональных данных

Это предварительный шаблон, который должен проверить юрист. Перед реальным запуском нужно проверить необходимость уведомления Роскомнадзора как оператора персональных данных.

Сервис обрабатывает данные компаний и клиентов только для регистрации, авторизации, учета покупок, выдачи подарков и поддержки. Согласия фиксируются в базе с датой, версией, IP и User-Agent. Пароли хэшируются, QR-код не содержит телефон, доступ к данным разграничен по компаниям.`;

export async function getSettings() {
  const db = getDb();
  const existing = await db.serviceSettings.findUnique({ where: { id: "default" } });

  if (existing) {
    return existing;
  }

  return db.serviceSettings.create({
    data: {
      id: "default",
      subscriptionPrice: 499,
      trialDays: 14,
      offerVersion: "1.0",
      offerText: defaultOfferText,
      privacyVersion: "1.0",
      privacyText: defaultPrivacyText,
      paymentRequisites: "",
      supportEmail: "rf173@bk.ru",
    },
  });
}
